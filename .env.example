# Interview Studio Docker Environment Configuration

# Database credentials (change in production!)
DB_USER=ai
DB_PASS=ai123

# JWT Secret (MUST change in production - minimum 32 characters)
JWT_SECRET=your-production-secret-key-must-be-at-least-32-characters-long

# OpenAI API Key (required for AI features)
OPENAI_API_KEY=sk-your-openai-api-key

# Frontend URL for CORS (adjust based on your deployment)
FRONTEND_BASE_URL=http://localhost:4200

# Gmail SMTP Configuration (for sending assessment results)
# 1. Enable 2-Step Verification in your Google account
# 2. Go to Google Account > Security > App passwords
# 3. Generate a new app password for "Mail"
GMAIL_USERNAME=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
EMAIL_ENABLED=true
